{{<html}}
{{$main}}

<div class="wrapper">

    <div class="extra-space"> </div>

    <div class="tag-title">
        <h1>All authors<h1>
    </div>

    <div class="extra-space"> </div>

    {{#empty(site.context.authors)}}
    Empty.
    {{/empty(site.context.authors)}}
    {{^empty(site.context.authors)}}
    <div class="authors-list-grid">
    {{#site.context.authors}}
        {{> partials.author}}
    {{/site.context.authors}}
    </div>
    {{/empty(site.context.authors)}}

</div>

{{/main}}
{{/html}}
