{{<html}}
{{$main}}

<div class="wrapper">

    <div class="extra-space"> </div>

    <h1>All tags</h1>

    <div class="extra-space"> </div>

    {{& page.contents}}

    {{#empty(site.context.tags)}}
    Empty.
    {{/empty(site.context.tags)}}
    {{^empty(site.context.tags)}}
    <div class="grid grid-421">
    {{#site.context.tags}}
        {{> partials.tag}}
    {{/site.context.tags}}
    
    </div>
    {{/empty(site.context.tags)}}

</div>

{{/main}}
{{/html}}
