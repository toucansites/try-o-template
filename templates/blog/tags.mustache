{{<html}}
{{$main}}

<section id="tags">

    {{& page.contents}}

    {{#empty(site.context.tags)}}
    Empty.
    {{/empty(site.context.tags)}}
    {{^empty(site.context.tags)}}
    <div class="grid grid-421">
    {{#site.context.tags}}
        {{> blog.partials.tag}}
    {{/site.context.tags}}
    
    </div>
    {{/empty(site.context.tags)}}
</section>

{{/main}}
{{/html}}
